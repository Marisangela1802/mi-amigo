import { StyleSheet } from "react-native";

const estiloDetalhesPet = StyleSheet.create({
    tela:{
        flex: 1,
        alignItems: "center",
        marginTop: 20
    },
    texto:{
        marginBottom: 8
    }
})

export default estiloDetalhesPet